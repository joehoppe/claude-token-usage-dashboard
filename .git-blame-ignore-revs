# Revisions that changed formatting only. `git blame` skips these so a line
# is attributed to the commit that wrote its content, not the one that
# rewrapped it.
#
# GitHub reads this file automatically. Locally it takes one opt-in:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs

# Format the tree with ruff format
5f9e8571ff2a1861112fe6acc0ca1de315562034
